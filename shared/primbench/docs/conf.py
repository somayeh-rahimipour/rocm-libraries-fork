# Configuration file for the Sphinx documentation builder.
#
# Generated by ROCm Documentation Generator.
# pylint: disable=invalid-name

# -- Project information ---------------------------------------------------

project = "Primbench"
author = "Advanced Micro Devices, Inc."
# pylint: disable=redefined-builtin
copyright = "Copyright (c) Advanced Micro Devices, Inc. All rights reserved."
# pylint: enable=redefined-builtin
# TODO: derive the version from this component's public header (see
# sibling components such as amdsmi or rccl) so the docs render a
# version number. No *_VERSION_MAJOR macro was auto-detected.

# -- General configuration -------------------------------------------------

html_theme = "rocm_docs_theme"
html_theme_options = {"flavor": "rocm"}
html_title = "Primbench documentation"
suppress_warnings = ["etoc.toctree"]
external_toc_path = "./sphinx/_toc.yml"
external_projects_current_project = "primbench"
extensions = [
    "rocm_docs",
    "sphinx_design",
    "breathe",
]
exclude_patterns = ["README.md"]

# -- Doxygen configuration -----------------------------------------------

# Doxygen XML is pre-generated by the RTD pre_build step (see
# .readthedocs.yaml). Plain breathe reads that committed output rather
# than rocm_docs.doxygen, which re-runs Doxygen from the Sphinx source
# directory and overwrites the XML with symbol-less output.
breathe_projects = {"Primbench API reference": "doxygen/xml"}
breathe_default_project = "Primbench API reference"

# -- Sphinx setup ----------------------------------------------------------


def setup(app):
    """Sphinx setup"""
    return {"parallel_read_safe": True, "parallel_write_safe": True}
