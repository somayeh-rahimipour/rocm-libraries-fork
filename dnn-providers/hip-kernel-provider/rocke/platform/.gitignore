# Keep the rocKE Beads Rust project visible even when a developer has a broad
# local `.beads/` exclude. Runtime database files remain ignored by
# `.beads/.gitignore`.
!.beads/
!.beads/.gitignore
!.beads/config.yaml
!.beads/issues.jsonl
!.beads/metadata.json
