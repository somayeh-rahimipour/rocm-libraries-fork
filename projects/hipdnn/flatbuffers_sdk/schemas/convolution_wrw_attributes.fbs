// Copyright © Advanced Micro Devices, Inc., or its affiliates.
// SPDX-License-Identifier: MIT

include "convolution_common.fbs";

// See graph.fbs for the semantics of this annotation.
attribute "cache_uid";

namespace hipdnn_flatbuffers_sdk.data_objects;

table ConvolutionWrwAttributes {
    // Input/Output tensor UIDs
    x_tensor_uid: long (cache_uid);          // Input tensor
    dy_tensor_uid: long (cache_uid);         // Gradient of output tensor (DY)
    dw_tensor_uid: long (cache_uid);         // Gradient of weights tensor (DW)

    // Convolution parameters (N-dimensional support)
    pre_padding: [long];         // Padding before each dimension
    post_padding: [long];        // Padding after each dimension
    stride: [long];              // Stride for each dimension
    dilation: [long];            // Dilation for each dimension

    // Algorithm/mode selection
    conv_mode: ConvMode;
}
