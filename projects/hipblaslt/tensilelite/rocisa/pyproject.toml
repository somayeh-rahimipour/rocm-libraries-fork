# Copyright © Advanced Micro Devices, Inc., or its affiliates.
# SPDX-License-Identifier: MIT

[project]
name = "rocisa"
version = "0.1.0"
description = "A C++ code generator for ROCm ISA"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[build-system]
# nanobind 3.0.0 does not build with clang on Windows: NB_SLOT_ALIAS constexpr-
# initialises a function pointer to the dllimport'd PyObject_Vectorcall, which is not a
# constant expression there. Fixed upstream by wjakob/nanobind#1421, so exclude just that
# release rather than capping the 3.x line -- 3.0.1 will be picked up automatically.
requires = ["scikit-build-core>=0.10", "nanobind>=2.0,!=3.0.0"]
build-backend = "scikit_build_core.build"

[tool.scikit-build]
cmake.build-type = "Release"
cmake.args = ["-DHIPBLASLT_ENABLE_YAML=ON", "-DCMAKE_POLICY_VERSION_MINIMUM=3.5"]
# No pure-Python packages to scan; the wheel contents come entirely from
# cmake install() rules (_rocisa.so + __init__.py → rocisa/).
wheel.packages = []
# Persistent build tree so repeated editable installs recompile incrementally
# instead of from scratch. This makes the rocisa rebuild that `invoke
# build-client` triggers a fast no-op when nothing changed. {wheel_tag} keeps
# per-interpreter/ABI build trees separate.
build-dir = "build/{wheel_tag}"

# Compatibility default. Rebuild-on-import is deprecated because it makes an
# ordinary import perform a native build; opt out now with
# SKBUILD_EDITABLE_REBUILD=false (or `invoke rocisa --no-rebuild-on-import`).
# A future release will default this to false after users have migrated to the
# explicit rebuild task.
editable.rebuild = true
editable.verbose = false
