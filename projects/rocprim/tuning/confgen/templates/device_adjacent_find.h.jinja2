{% extends "common/base.h.jinja2" %}

{% block target_tparams %}
template<class Target, class value_type>
{% endblock %}

{% block config_guard scoped %}({% if not type.key_type.is_floating %}!{% endif %}rocprim::is_floating_point<value_type>::value){% if type.key_type.size_min > 0 %} && (sizeof(value_type) > {{ type.key_type.size_min }}){% endif %} && (sizeof(value_type) <= {{ type.key_type.size }}){% endblock %}

{% block config_constructor -%}
adjacent_find_config_params{ { {{ config.block_size_x }}, {{ config.items_per_thread }} } }
{%- endblock %}

{% block config_base_type -%}
adjacent_find_config_params_base<value_type>
{%- endblock %}
